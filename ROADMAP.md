# 🗺️ 开发路线图 / 优化建议

本文档记录已读追踪项目的待优化项，按优先级分类。

## 🟡 中优先级（体验优化）

### 1. 隧道重连机制
- **问题**: cloudflared 掉线后没有自动重连，悬浮仪表盘需要手动点击重启
- **方案**: 检测到隧道进程退出 → 自动重启 + 指数退避
- **影响范围**: Java 插件版服务端

### 2. IP 去重的局限
- **问题**: 同一局域网多设备算一人，切换网络可能重复计数
- **方案**: 添加 `X-Forwarded-For` 链解析 + 更精细的设备指纹（UA hash）
- **当前状态**: Python 服务端已支持 X-Forwarded-For；可进一步添加 UA hash 辅助去重

### 3. 消息 ID 碰撞
- **问题**: 消息 ID 只用 `SHA-256(wxId + content + createTime)`，极端情况下同毫秒两条消息会碰撞
- **方案**: 添加随机盐或自增序号
- **影响范围**: 所有服务端实现

## 🟢 低优先级（锦上添花）

### 4. 统计面板增强
- 控制台添加「今日已读数 / 总数 / 峰值」趋势图

### 5. 数据导出
- SQLite 导出 CSV/JSON，方便备份

### 6. 多隧道节点
- cloudflared 慢时自动切换 serveo.net（JSch 方案已内置但未启用）

## ✅ 已完成

- [x] 修复 DEX 缓存更新问题（WeKit 模块）
- [x] 修复字体文件服务路由死代码（Python 服务端）
- [x] Python 服务端支持 X-Forwarded-For 获取真实 IP
- [x] CI 自动构建 APK + Java 插件并发布到 Releases
- [x] 仓库结构优化：客户端 / 服务器两大分类
